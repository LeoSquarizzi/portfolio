---
import DragonEasterEgg from '../components/DragonEasterEgg.astro';
import NinjaAnimation from '../components/NinjaAnimation.astro';

interface Props {
  title?: string;
  description?: string;
  image?: string;
  keywords?: string;
  canonicalUrl?: string;
}

const {
  title = 'Leonardo Squarizzi - Game Design & Game Dev',
  description = 'Game Designer and Indie Game Developer',
  image = '/img/icon.jpg',
  keywords = 'open source, web development, mobile development, game development, react native, unity, phaser, typescript, javascript',
  canonicalUrl
} = Astro.props;

const siteUrl = 'https://leosquarizzi.com';
const currentPath = Astro.url.pathname;
const fullUrl = canonicalUrl || `${siteUrl}${currentPath}`;
const fullImageUrl = image.startsWith('http') ? image : `${siteUrl}${image}`;
---

<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <meta http-equiv="X-UA-Compatible" content="ie=edge" />
    <title>{title}</title>

    <!-- SEO Meta Tags -->
    <meta name="description" content={description} />
    <meta name="keywords" content={keywords} />
    <meta name="author" content="Leonardo Squarizzi" />
    <meta name="robots" content="index, follow" />
    <meta name="googlebot" content="index, follow" />
    <link rel="canonical" href={fullUrl} />

    <!-- Favicons -->
    <link rel="apple-touch-icon" sizes="180x180" href="/apple-touch-icon.png" />
    <link rel="icon" type="image/png" sizes="32x32" href="/logo.png" />
    <link rel="icon" type="image/png" sizes="16x16" href="/logo.png" />
    <link rel="manifest" href="/site.webmanifest" />
    <link rel="mask-icon" href="/safari-pinned-tab.svg" color="#f2385a" />
    <meta name="msapplication-TileColor" content="#0a0a0b" />
    <meta name="theme-color" content="#0a0a0b" />

    <!-- Open Graph / Facebook -->
    <meta property="og:type" content="website" />
    <meta property="og:locale" content="en_US" />
    <meta property="og:site_name" content="Leonardo Squarizzi" />
    <meta property="og:title" content={title} />
    <meta property="og:url" content={fullUrl} />
    <meta property="og:description" content={description} />
    <meta property="og:image" content={fullImageUrl} />
    <meta property="og:image:width" content="1200" />
    <meta property="og:image:height" content="630" />
    <meta property="og:image:alt" content="Leonardo Squarizzi - Game Design & Game Dev" />

    <!-- Twitter -->
    <meta name="twitter:card" content="summary_large_image" />
    <meta name="twitter:title" content={title} />
    <meta name="twitter:description" content={description} />
    <meta name="twitter:site" content="@proyecto26" />
    <meta name="twitter:creator" content="@jdnichollsc" />
    <meta name="twitter:image" content={fullImageUrl} />
    <meta name="twitter:image:alt" content="Leonardo Squarizzi - Game Design & Game Dev" />

    <!-- Preconnect to external resources -->
    <link rel="preconnect" href="https://fonts.googleapis.com" />
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin />

    <!-- Lucide Icons -->
    <script src="https://unpkg.com/lucide@latest/dist/umd/lucide.min.js" defer></script>

    <!-- Global site tag (gtag.js) - Google Analytics -->
    <script async src="https://www.googletagmanager.com/gtag/js?id=G-4ND3N4W941"></script>
    <script is:inline>
      window.dataLayer = window.dataLayer || [];
      function gtag() { dataLayer.push(arguments); }
      gtag('js', new Date());
      gtag('config', 'G-4ND3N4W941');
    </script>
  </head>
  <body class="bg-bg-primary text-text-primary antialiased">
    <!-- Custom cursor (desktop only) -->
    <div class="cursor-dot hidden lg:block"></div>
    <div class="cursor-outline hidden lg:block"></div>

    <slot />

    <!-- Dragon Easter Egg -->
    <DragonEasterEgg />

    <!-- Ninja Animation -->
    <NinjaAnimation />

    <!-- Initialize Lucide icons -->
    <script>
      document.addEventListener('DOMContentLoaded', () => {
        if (window.lucide) {
          window.lucide.createIcons();
        }
      });
    </script>

    <script>
      import { gsap } from 'gsap';

      // Custom cursor
      const cursorDot = document.querySelector('.cursor-dot');
      const cursorOutline = document.querySelector('.cursor-outline');

      if (cursorDot && cursorOutline && window.matchMedia('(hover: hover)').matches) {
        window.addEventListener('mousemove', (e) => {
          gsap.to(cursorDot, {
            x: e.clientX,
            y: e.clientY,
            duration: 0.1,
          });
          gsap.to(cursorOutline, {
            x: e.clientX,
            y: e.clientY,
            duration: 0.3,
          });
        });

        // Expand cursor on interactive elements
        document.querySelectorAll('a, button, [data-cursor]').forEach((el) => {
          el.addEventListener('mouseenter', () => {
            gsap.to(cursorOutline, { scale: 2, opacity: 1, duration: 0.3 });
            gsap.to(cursorDot, { scale: 0.5, duration: 0.3 });
          });
          el.addEventListener('mouseleave', () => {
            gsap.to(cursorOutline, { scale: 1, opacity: 0.5, duration: 0.3 });
            gsap.to(cursorDot, { scale: 1, duration: 0.3 });
          });
        });
      }
    </script>
  </body>
</html>

<style is:global>
  @import '../styles/global.css';
</style>
